{
  "name": "participle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/logic.test.js tests/participle-spec.test.js",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "test:runtime": "playwright test",
    "start": "py -m http.server 4173 -d dist"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
